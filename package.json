{
  "name": "store",
  "scripts": {
    "dev:server": "pnpm --filter=@store/server run dev",
    "dev:web": "pnpm --filter=@store/web run dev",
    "lint": "pnpm --recursive --parallel --stream run lint"
  }
}
